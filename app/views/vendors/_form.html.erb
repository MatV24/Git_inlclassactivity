<%= form_with(model: vendor, local: true) do |form| %>
  <% if vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vendor.errors.count, "error") %> prohibited this vendor from being saved:</h2>

      <ul>
      <% vendor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :vendorname %>
    <%= form.text_field :vendorname %>
  </div>

  <div class="field">
    <%= form.label :productname %>
    <%= form.text_field :pricing, product.productname.all %>
  </div>

  <div class="field">
    <%= form.label :pricing %>
    <%= form.text_field :pricing, productname %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
